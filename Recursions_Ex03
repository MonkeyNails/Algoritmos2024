# 3. Faça uma função recursiva para exibir os números compreendidos entre a e b,
#  sendo a e b parâmetros da função.

def exibir_numeros_meio(a, b):
    if a > b:
        exibir_numeros_meio(a - 1, b)
        if b == a - 1:
            print(b)
        print(a)
    if b > a:
        exibir_numeros_meio(a, b - 1)
        if a == b - 1:
            print(a)
        print(b)


exibir_numeros_meio(30, 74)
